name: claude-code
version: "1.0.0"
tier: shell
tags:
  - coding-agent
  - orchestration
  - cli
  - model-locked:claude
  - multi-agent
setup: |
  # Claude Code must be installed: npm install -g @anthropic-ai/claude-code
  which claude || echo "Claude Code not found. Install with: npm install -g @anthropic-ai/claude-code"
run: ./adapter.sh
model_default: claude-opus-4-6
